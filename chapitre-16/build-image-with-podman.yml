---

- name: "Build image with Podman"
  hosts: localhost
  gather_facts: false
  collections:
    - containers.podman
  tasks:
    - name: "Build rocky-httpd image"
      podman_image:
        name: "rocky-httpd"
        path: "rocky-httpd"
        force: true
