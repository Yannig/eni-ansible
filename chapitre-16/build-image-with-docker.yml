---

- name: "Build docker images"
  hosts: localhost
  gather_facts: false
  tasks:
    - name: "Build rocky-httpd image"
      docker_image:
        name: "rocky-httpd"
        source: build
        force_source: true
        build:
          path: "rocky-httpd"
          pull: true
