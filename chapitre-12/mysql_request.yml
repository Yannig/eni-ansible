---

- name: "Table count"
  hosts: mysql
  gather_facts: false
  tasks:
    - name: "Retrieve table count"
      mysql_request:
        db_name: "mediawiki"
        request: "{{ lookup('file', 'test.sql') }}"
    - debug: var=ansible_facts.mysql_results
