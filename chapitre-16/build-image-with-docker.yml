---

- name: "Build docker images"
  hosts: localhost
  gather_facts: no
  tasks:
    - name: "Build rocky-httpd image"
      docker_image:
        name: "rocky-httpd"
        source: build
        force_source: yes
        build:
          path: "rocky-httpd"
          pull: yes
