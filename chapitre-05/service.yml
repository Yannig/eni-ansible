---

- name: "Install Grafana"
  hosts: all
  gather_facts: false
  tasks:
    - name: "Install NSSM package (service handling)"
      chocolatey.chocolatey.win_chocolatey:
        name: "nssm"
        state: present
